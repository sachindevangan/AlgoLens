export default function PatternsPage() {
  return (
    <div className="max-w-4xl">
      <h1 className="text-3xl font-semibold text-white">Patterns</h1>
      <p className="mt-2 text-muted">
        Browse common DSA patterns (sliding window, two pointers, graphs) and
        open curated examples with step-by-step visual traces.
      </p>
    </div>
  );
}

